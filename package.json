{
  "name": "synapse-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "npm run clean && npm run build && node --test dist/test/unit/*.test.js dist/test/integration/*.test.js",
    "test:unit": "npm run clean && npm run build && node --test dist/test/unit/*.test.js",
    "test:integration": "npm run clean && npm run build && node --test dist/test/integration/*.test.js",
    "mcp:start": "npm run clean && npm run build && node dist/index.js",
    "runner:start": "npm run clean && npm run build && node dist/runner.js start",
    "runner:doctor": "npm run clean && npm run build && node dist/runner.js doctor"
  },
  "bin": {
    "synapse-mcp": "./dist/index.js",
    "synapse-runner": "./dist/runner.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.7.3"
  }
}
